apiVersion: v1
kind: Service
metadata:
  name: {{ service_name }}-svc
spec:
  selector:
    app: {{ service_name }}
  ports:
    - protocol: TCP
      port: {{ container_port }}
      targetPort: {{ container_port }}
  type: ClusterIP