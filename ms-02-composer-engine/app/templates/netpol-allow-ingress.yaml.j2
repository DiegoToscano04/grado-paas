apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-external-to-{{ service_name }}
spec:
  podSelector:
    matchLabels:
      app: {{ service_name }}
  policyTypes:
  - Ingress
  ingress:
  - ports:
    - protocol: TCP
      port: {{ container_port }}